[package]
name = "corpus-integration"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
corpus-admin = { path = "../corpus-admin" }
corpus-app = { path = "../corpus-app", default-features = false }
corpus-core = { path = "../corpus-core" }
corpus-mcp = { path = "../corpus-mcp" }
corpus-model-test = { path = "../corpus-model-test" }
corpus-store = { path = "../corpus-store" }
serde = { workspace = true }
serde_json = { workspace = true }
